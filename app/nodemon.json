{
  "restartable": "rs",
  "events": {
    "restart": "npm test && osascript -e 'display notification \"App restarted due to:\n'$FILENAME'\" with title \"nodemon\"'"
  },
  "watch": [
    "/server/*"
  ],

  "ignore": [
    ".git",
    "/server/**/**/*.test.js",
    "/node_modules/**/node_modules",
    "/server/public/*"
  ]

}
